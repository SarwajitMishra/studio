rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // User avatars are publicly readable, but only writable by the owner.
    match /avatars/{userId}/{allPaths=**} {
      allow read;
      allow write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
